CREATE TABLE `user_info`(
   `id` int,
   `name` string,
   `nick_name` string);

+---------------+-----------------+----------------------+--+
| user_info.id  | user_info.name  | user_info.nick_name  |
+---------------+-----------------+----------------------+--+
| 1             | Nick            | nicky                |
| 2             | Alice           | ali                  |
+---------------+-----------------+----------------------+--